{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Reset HRMS Tables",
      "type": "shell",
      "command": "sqlcmd -S . -d HRMS -E -i drop_script.sql"
    },
    {
      "label": "Load Schema",
      "type": "shell",
      "command": "sqlcmd -S . -d HRMS -E -i schema.sql"
    },
    {
      "label": "Insert Data",
      "type": "shell",
      "command": "sqlcmd -S . -d HRMS -E -i insert_data.sql"
    },
    {
      "label": "Full Reset (Reset + Schema + Insert)",
      "dependsOn": [
        "Reset HRMS Tables",
        "Load Schema",
        "Insert Data"
      ],
      "dependsOrder": "sequence",
      "type": "shell",
      "command": "echo Full reset completed.",
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
